// console.log(1, 2);
// console.log('zhixing')

// class B {

// };

// function * gen (params) {
//   yield 1
// };

// 'aaa'.includes('a');
// console.log('aaa'.includes('a'))

// console.log(gen().next());

const sum = (a, b) => {
  return a + b + 'sum'
};

const mins = (a, b) => {
  return a- b + 'mins'
};

export default {
  sum,
  mins
};
